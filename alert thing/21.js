const hiddenElements = document.querySelectorAll('.hidden');

const shownElements = IntersectionObserver((entries) =>{
    entries.forEach(entries => {
        console.log(entries)
        if(entry.){
            target
        }
    });
})